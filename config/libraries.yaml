libraries:
  - name: TV
    type: series
    root: /media/storage/data/media/tv

  - name: Anime
    type: anime
    root: /media/storage/data/media/anime

  - name: DocuSeries
    type: docuseries
    root: /media/storage/data/media/documentaries_tv

  - name: Documentaries
    type: documentary
    root: /media/storage/data/media/documentaries_mov

  - name: Movies
    type: movies
    root: /media/storage/data/media/movies

  - name: Movies3D
    type: movies
    root: /media/storage/data/media/movies_3d

download:
  # Optional: dedicated TDL session directory to avoid DB locks with other clients.
  # tdl_home: /home/ukiyo/.tdl-plexbot
  # tdl_template: 'tdl dl -u {url} -d "{dir}" -t 16 -l 9 --reconnect-timeout 0 --template "{{ .FileName }}"'

# Optional UI overrides for category buttons.
ui:
  categories:
    series: "📺 Series"
    anime: "✨ Anime"
    docuseries: "🎞️ Docuseries"
    documentary: "🎥 Documentales"
    movies: "🎬 Peliculas"
